{
  "name": "red-packet-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio red-packet-sepolia",
    "create-local": "graph create --node http://localhost:8020/ red-packet-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ red-packet-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 red-packet-sepolia"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
